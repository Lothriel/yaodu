---
- include: common.yml
  tags: common, openstack_common, ceph, mariadb, haproxy, keepalived

- include: openstack_common.yml
  tags: openstack_common

- include: ceph.yml
  tags: ceph

- include: mariadb.yml
  tags: mariadb

- include: haproxy.yml
  tags: haproxy

- include: keepalived.yml
  tags: keepalived
