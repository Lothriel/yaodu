---
- name: Ensuring Docker image yaodu/{{ container }} exists
  docker_image_check:
    image: "yaodu/{{ container }}"
    registry: "{{ docker_registry }}"
    protocol: "http"
    insecure_registry: True
  run_once: True
  when: not forced

- include: build.yml
  when: forced or not docker.image_exists
